import { defineStore } from "pinia";
import { ref, computed } from "vue";

export const useParams = defineStore(
  "params",
  () => {
    const listOfParamsDate = ref([
        {
            
        }
    ]);

    // getter

    // action

    // data

    // params

    const $reset = () => {};

    return {};
  },
  {
    persist: true,
  }
);
